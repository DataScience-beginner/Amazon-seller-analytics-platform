services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: selleros
      POSTGRES_USER: selleros
      POSTGRES_PASSWORD: selleros_local_password
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
